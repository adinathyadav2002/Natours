doctype html 
html 
    head 
        block head
            meta(charset='UTF-8')
            meta(http-equiv="Content-Security-Policy" content="script-src 'self' https://cdnjs.cloudflare.com;")
            meta(name='viewport' content='width=device-width, initial-scale=1.0')
            link(rel='stylesheet' herf='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
            link(rel='stylesheet' href='/css/style.css')
            link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
            title Natours | #{title}
    body 
        // HEADER
        include _header.pug

        // CONTENT
        block content

        // FOOTER
        include _footer.pug

        script(src='/js/bundle.js')

        //-*************************************************************
        //- Instead of using these three separate file we use bundler to bundle all the files from 
        //- from all three file or index.js to bundle.js
        //- script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.7.8/axios.min.js')
        //- script(src='/js/mapbox.js')
        //- script(src='/js/login.js')
        //-*************************************************************

        //-*************************************************************
            -- PUG USES INDENTATION LIKE PYTHON
            h1= tour
            //h1 The Forest Hiker       -- REGULAR COMMENT WILL SHOW ON WEBPAGE
            //-h1 The Forest Hiker      -- ONLY PRESENT HERE
            p Adinath #{sirname}        -- THIS IS STRING INTERPOLATION

            - const x = 100;            -- WRITING JS
            h3= x *100                  -- USE ASSIGNMENT OPERATOR WITHOUT SPACE AFTER ELEMENT
        //-*************************************************************